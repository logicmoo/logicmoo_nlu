:-include(library('ec_planner/ec_test_incl')).
:-expects_dialect(pfc).
 %  loading(always,'includes/SaaSPatterns/sorts.e').

% sort rule,subject,object,action,ruleeffect,policy,policyset
==> sort(rule).
==> sort(subject).
==> sort(object).
==> sort(action).
==> sort(ruleeffect).
==> sort(policy).
==> sort(policyset).
